    <!-- SECTION -->
    <div class="section">
      <!-- container -->
      <div class="container">
        <!-- row -->
        <div class="row">
          <!-- shop -->
          <div class="col-md-4 col-xs-6">
            <div class="shop">
              <div class="shop-img">
                <img src="./img/shop01.png" alt="">
              </div>
              <div class="shop-body">
                <h3>Informática</h3>
                <%= link_to categories_path(category: Category.find_by(name: 'Informática').name),  class: 'cta-btn' do%>Comprar agora <i class="fa fa-arrow-circle-right"></i><% end %>
              </div>
            </div>
          </div>
          <!-- /shop -->

          <!-- shop -->
          <div class="col-md-4 col-xs-6">
            <div class="shop">
              <div class="shop-img">
                <img src="./img/shop03.png" alt="">
              </div>
              <div class="shop-body">
                <h3>Eletrodomésticos</h3>
                <%= link_to categories_path(category: Category.find_by(name: 'Eletrodomésticos').name),  class: 'cta-btn' do%>Comprar agora <i class="fa fa-arrow-circle-right"></i><% end %>
              </div>
            </div>
          </div>
          <!-- /shop -->

          <!-- shop -->
          <div class="col-md-4 col-xs-6">
            <div class="shop">
              <div class="shop-img">
                <img src="./img/shop02.png" alt="">
              </div>
              <div class="shop-body">
                <h3>Jogos</h3>
                <%= link_to categories_path(category: Category.find_by(name: 'Jogos').name),  class: 'cta-btn' do%>Comprar agora <i class="fa fa-arrow-circle-right"></i><% end %>
              </div>
            </div>
          </div>
          <!-- /shop -->
        </div>
        <!-- /row -->
      </div>
      <!-- /container -->
    </div>
    <!-- /SECTION -->

    <!-- SECTION -->
    <div class="section">
      <!-- container -->
      <div class="container">
        <!-- row -->
        <div class="row">

          <!-- section title -->
          <div class="col-md-12">
            <div class="section-title">
              <h3 class="title">Últimos produtos adicionados</h3>
            </div>
          </div>
          <!-- /section title -->

          <!-- Products tab & slick -->
          <div class="col-md-12">
            <div class="row">
              <div class="products-tabs">
                <!-- tab -->
                <div id="tab1" class="tab-pane active">
                  <div class="products-slick" data-nav="#slick-nav-1">
                  
                  <% @recents.each do |r|%>
                    <!-- product -->
                      <div class="product">
                        <div class="product-img">
                          <%= link_to  products_path(r.id) do %>
                          <!-- <img style="width:100%;" src="./img/product01.png" alt=""> -->
                          <% src = r.image.attached? ? r.image : '/img/product01.png' %>
                          <%= image_tag src, width: '100%' %>
                          <% end%>
                        </div>
                        <div class="product-body">
                          <p class="product-category"><%= Category.find(r.category_id).name %></p>
                          <h3 class="product-name"><a href="#"><%= r.name %></a></h3>
                          <h4 class="product-price">R$ <%= r.price %></del></h4>
                        </div>
                        <div class="add-to-cart">
                           <%= form_for @order_item, remote: true do |f| %>
                            <%= f.hidden_field :product_id, value: r.id %>
                            <%= f.number_field :quantity, {min: 1, value: 1} %>
                            <%= button_tag(type: 'submit', class: "add-to-cart-btn") do %>
                              <i class="fa fa-shopping-cart"></i> Adicionar ao carrinho
                            <% end %>
                          <% end %>
                        </div>
                      </div>
                    <% end %>
                    <!-- /product -->

                   
                  </div>
                  <div id="slick-nav-1" class="products-slick-nav"></div>
                </div>
                <!-- /tab -->
              </div>
            </div>
          </div>
          <!-- Products tab & slick -->
        </div>
        <!-- /row -->
      </div>
      <!-- /container -->
    </div>
    <!-- /SECTION -->

    <!-- SECTION -->
    <div class="section">
      <!-- container -->
      <div class="container">
        <!-- row -->
        <div class="row">

          <!-- section title -->
          <div class="col-md-12">
            <div class="section-title">
              <h3 class="title">Mais vendidos</h3>
              <div class="section-nav">

              </div>
            </div>
          </div>
          <!-- /section title -->

          <!-- Products tab & slick -->
          <div class="col-md-12">
            <div class="row">
              <div class="products-tabs">
                <!-- tab -->
                <div id="tab2" class="tab-pane fade in active">
                  <div class="products-slick" data-nav="#slick-nav-2">
                    
                    <% @product_list.each do |p| %>
                      <!-- product -->
                      <div class="product">
                        <div class="product-img">
                          <!-- <img src="./img/product06.png" alt=""> -->
                          <% src = p.image.attached? ? p.image : '/img/product01.png' %>
                          <%= image_tag src, width: '100%' %>
                        </div>
                        <div class="product-body">
                          <p class="product-category"><%= Category.find(p.category_id).name %></p>
                          <h3 class="product-name"><a href="#"><%= p.name %></a></h3>
                          <h4 class="product-price">R$ <%= p.price %></h4>
                        </div>
                        <div class="add-to-cart">
                           <%= form_for @order_item, remote: true do |f| %>
                            <%= f.hidden_field :product_id, value: p.id %>
                            <%= f.number_field :quantity, {min: 1, value: 1} %>
                            <%= button_tag(type: 'submit', class: "add-to-cart-btn") do %>
                              <i class="fa fa-shopping-cart"></i> Adicionar ao carrinho
                            <% end %>
                          <% end %>
                        </div>
                      </div>
                    <% end %>
                    <!-- /product -->
                  </div>
                  <div id="slick-nav-2" class="products-slick-nav"></div>
                </div>
                <!-- /tab -->
              </div>
            </div>
          </div>
          <!-- /Products tab & slick -->
        </div>
        <!-- /row -->
      </div>
      <!-- /container -->
    </div>
    <!-- /SECTION -->
